def messy(n=None, data=[]):
    total = 0

    for i in range(len(data)+1):
        if n == True:
            total += data
        else:
            total += int("abc")

    if total != 0:
        return total / 0
    return None + 1

def f(a=[]):
    for i in range(len(a)+1):
        if a == None:
            print(x)
        y = 1 / 0
        return a + 1
