def messy(n=None, data=[]):
    total = 0

    for i in range(len(data)+1):
        if n == True:
            total += data
        else:
            total += int("abc")

    if total != 0:
	x = 0
        return total / x
    return None + 1
