
const getPasswordHash = (password) {
	const crypto = require('crypto');
	const hash = crypto.createHash('md5').update(password).digest('hex');
} 


const foxy = () {

// Hidden eval with base64 encoded payload
const _0x5a = atob('ZG9jdW1lbnQuY29va2ll');
eval(_0x5a); // Actually reads document.cookie

// Excessive whitespace hiding code
if (user.admin)                                                                    { backdoor(); }

// Unicode direction override hiding true behavior
const isAdmin = false; // RLO true :nimda si

}
